import { Achievement } from '@/types/content';

/**
 * /mock/achievements.ts
 *
 * Intentionally empty — no confirmed content yet (milestones excluded per
 * client request; certifications/awards still unconfirmed). Left in place,
 * fully typed, so this can be populated later without touching content.ts,
 * lib/content/achievements.ts, or any component that imports
 * getAchievements()/getLatestAchievements().
 */

export const achievements: Achievement[] = [];